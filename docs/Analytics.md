# Analytics

Recommendation analytics are persisted through `Backend/application/recommendation_store.py`.

Tracked metrics include recommendation counts, executed/skipped/blocked trades, wins/losses, precision, recall, false positives, false negatives, profit factor, expectancy, drawdown, average RR, best/worst setup, best/worst strategy, confidence vs win rate, quality vs win rate, strategy vs outcome, and regime vs outcome.
