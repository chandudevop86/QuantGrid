# Analytics

Recommendation analytics are persisted through `Backend/application/recommendation_store.py`.

Tracked metrics include recommendation counts, executed/skipped/blocked trades, wins/losses, precision, recall, false positives, false negatives, profit factor, expectancy, drawdown, average RR, best/worst setup, confidence vs win rate, and quality vs win rate.

